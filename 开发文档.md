# VCBHW 开发文档（MFC 单摆仿真 + WMP ActiveX 播放）

> 仓库：C-sin/VCBHW

## 1. 项目概述
- 项目名称：VCBHW
- 开发环境：Visual Studio（MFC，Windows）
- 语言：C++（约 91.6%）、C（约 8.4%）
- 项目目标：
  1. 建立单文档（SDI）程序，包含菜单/工具栏等交互；
  2. 通过菜单项打开“参数设置”对话框，对摆球尺寸、摆线长度、初始释放角度、阻尼系数进行配置；
  3. 在主视图中进行单摆（含阻尼）仿真、动画绘制，并加入残影效果；
  4. 通过菜单项打开“播放”对话框，插入 ActiveX（Windows Media Player）播放 `Narration.avi`。

---

## 2. 工程结构与模块划分
- 视图层：`VCBHWView.h/.cpp`
  - 绘制单摆与残影
  - 处理菜单命令
  - 定时器驱动仿真更新
- 参数设置对话框：`CParamDlg.h/.cpp`
  - 控件联动（Radio/Combo/Slider/ScrollBar/Spin/Edit）
  - 输出最终参数（m_finalSize/m_finalLength/m_finalAngle/m_finalDamping）
- 播放对话框：`CPlayDlg.h/.cpp`
  - 内嵌 WMP ActiveX 控件（`IDC_OCX1`）
- ActiveX 封装：`COCX1.h`
  - WMP 控件的 MFC 封装（属性/方法封装）

---

## 3. 功能说明
### 3.1 单摆仿真
- 通过菜单进入参数设置，设置：
  - 摆球尺寸
  - 摆线长度
  - 初始释放角度
  - 阻尼系数
- 点击“开始运动”后启动仿真。
- 运动停止条件：达到指定时长或接近静止（阈值判断）。
- 绘制内容：天花板、摆线、摆球、残影轨迹、阴影。

### 3.2 视频播放（ActiveX / Windows Media Player）
- 从菜单触发“播放开发录像”，弹出播放对话框；
- 播放对话框内嵌 WMP ActiveX 控件（IDC_OCX1）；
- 视频文件 `Narration.avi` 建议放在 **可执行文件同目录**（例如：`x64\Debug\Narration.avi` / `x64\Release\Narration.avi`）；
- 播放时使用绝对路径设置 WMP URL，避免相对路径导致控件在“准备就绪/正在打开”间反复切换。

---

## 4. 资源对象清单

### 4.1 对象 ID 列表（ID）

#### 4.1.1 对话框（Dialog）
| 对象 | 资源 ID | 类 | Caption |
|---|---:|---|---|
| 参数设置对话框 | IDD_DIALOG_SETTINGS (311) | `CParamDlg` | 参数设置 |
| 播放对话框 | IDD_DIALOG_PLAY (313) | `CPlayDlg` | Dialog（可按需改名） |
| About 对话框 | IDD_ABOUTBOX (100) | （MFC 生成） | 关于 VCBHW |

#### 4.1.2 控件（Control）
| 控件 ID | 数值 |
|---|---:|
| IDC_COMBO1 | 1000 |
| IDC_RADIO1 | 1001 |
| IDC_RADIO2 | 1002 |
| IDC_RADIO3 | 1003 |
| IDC_SCROLLBAR1 | 1004 |
| IDC_SLIDER1 | 1005 |
| IDC_SLIDER2 | 1006 |
| IDC_EDIT1 | 1007 |
| IDC_SPIN1 | 1008 |
| IDC_COMBO2 | 1009 |
| IDC_BTN_START | 1010 |
| IDC_EDIT2 | 1011 |
| IDC_EDIT4 | 1013 |
| IDC_BUTTON1 | 1014 |
| IDC_BUTTON2 | 1015 |
| IDC_OCX1 | 1016 |

#### 4.1.3 菜单命令（Command）
| 菜单项 | 命令 ID |
|---|---:|
| 参数设置 | ID_SIM_SETTINGS (32773) |
| 播放开发录像 | ID_SIM_PLAYVIDEO (32774) |

---

### 4.2 变量列表（DDX / 成员变量）

#### 4.2.1 参数设置对话框 `CParamDlg`（IDD_DIALOG_SETTINGS）
| 控件 ID | 控件类型 | DDX/成员变量 | 数据变量（DDX_Text） | 说明 |
|---|---|---|---|---|
| IDC_RADIO1~IDC_RADIO3 | Radio | `m_nSizeMode`（DDX_Radio） | - | 摆球尺寸单选组（10/20/30） |
| IDC_COMBO1 | ComboBox | `m_comboSize`（DDX_Control） | - | 摆球尺寸下拉（40/50） |
| IDC_EDIT2 | EditText | - | `m_nSizeValue`（DDX_Text） | 当前尺寸值显示（只读） |
| IDC_SPIN1 | Spin | `m_spinAngle`（DDX_Control） | - | 初始角度（10~90） |
| IDC_SLIDER2 | Slider | `m_sliderLength`（DDX_Control） | - | 摆线长度（50~200） |
| IDC_SCROLLBAR1 | ScrollBar | `m_scrollLength`（DDX_Control） | - | 摆线长度滚动条（50~200） |
| IDC_EDIT4 | EditText | - | `m_nLengthValue`（DDX_Text） | 当前长度值显示（只读） |
| IDC_COMBO2 | ComboBox | `m_comboDamping`（DDX_Control） | - | 阻尼选项 |

**用于主窗口接收的最终参数：**
- `m_finalSize`：最终摆球尺寸
- `m_finalLength`：最终摆线长度
- `m_finalAngle`：最终初始角度
- `m_finalDamping`：最终阻尼系数

#### 4.2.2 播放对话框 `CPlayDlg`（IDD_DIALOG_PLAY）
| 控件 ID | 控件类型 | DDX/成员变量 | 说明 |
|---|---|---|---|
| IDC_OCX1 | WMP ActiveX | `m_wmp`（DDX_Control） | Windows Media Player 控件 |

---

### 4.3 响应函数列表（消息映射 / 事件处理）

#### 4.3.1 主视图 `CVCBHWView`
| 触发源 | ID/消息 | 响应函数 | 功能 |
|---|---|---|---|
| 菜单命令 | ID_SIM_SETTINGS | `CVCBHWView::OnSimSettings()` | 打开参数对话框，读回参数并启动定时器 |
| 菜单命令 | ID_SIM_PLAYVIDEO | `CVCBHWView::OnSimPlayvideo()` | 打开播放对话框 `CPlayDlg` |
| 窗口消息 | WM_TIMER | `CVCBHWView::OnTimer(UINT_PTR)` | 物理更新 + 残影收敛 + 重绘 |
| 窗口消息 | WM_ERASEBKGND | `CVCBHWView::OnEraseBkgnd(CDC*)` | 返回 TRUE 减少闪烁 |

#### 4.3.2 参数设置对话框 `CParamDlg`
| 控件/消息 | ID | 响应函数 | 功能 |
|---|---:|---|---|
| 单选按钮点击 | IDC_RADIO1 | `OnBnClickedRadio1()` | 尺寸=10，取消 Combo 选择 |
| 单选按钮点击 | IDC_RADIO2 | `OnBnClickedRadio2()` | 尺寸=20，取消 Combo 选择 |
| 单选按钮点击 | IDC_RADIO3 | `OnBnClickedRadio3()` | 尺寸=30，取消 Combo 选择 |
| 下拉框改变 | IDC_COMBO1 | `OnCbnSelchangeCombo1()` | 选择尺寸=40/50，取消 Radio |
| 滑块/滚动条 | WM_HSCROLL | `OnHScroll(...)` | Slider 与 ScrollBar 联动更新长度 |
| 按钮点击 | IDC_BTN_START | `OnBnClickedBtnStart()` | 提取最终参数并返回主窗口 |
| 下拉框改变 | IDC_COMBO2 | `OnCbnSelchangeCombo2()` | 映射并设置最终阻尼值 |

#### 4.3.3 播放对话框 `CPlayDlg`
| 控件/消息 | ID | 响应函数 | 功能 |
|---|---:|---|---|
| （暂未实现）按钮点击 | IDC_BUTTON1 | （无） | 预期：手动触发播放（当前版本未在代码中绑定响应函数） |
| （暂未实现）按钮点击 | IDC_BUTTON2 | （无） | 预期：关闭对话框（当前版本未在代码中绑定响应函数） |

> 说明：当前 `CPlayDlg` 的消息映射中未绑定 `IDC_BUTTON1/IDC_BUTTON2` 的点击响应；如果需要按钮生效，可在 `CPlayDlg` 中补充 `ON_BN_CLICKED` 映射，或使用默认 IDOK/IDCANCEL 机制实现关闭。

---

### 4.4 属性列表（范围、默认值、控件样式）

#### 4.4.1 参数设置对话框（运行时初始化属性）
| 控件 | 属性/范围 | 默认值 |
|---|---|---|
| IDC_SPIN1（角度） | 10~90 | 30 |
| IDC_SLIDER2（长度） | 50~200 | 100 |
| IDC_SCROLLBAR1（长度） | 50~200 | 100 |
| IDC_COMBO2（阻尼） | 无/弱/强/超强 | 未强制默认选择 |
| IDC_COMBO1（尺寸） | 添加 40/50 两项 | 未强制默认选择 |

#### 4.4.2 参数设置对话框
| 控件 | 类型 | 样式要点 |
|---|---|---|
| IDC_COMBO1 | COMBOBOX | `CBS_DROPDOWN`、`CBS_SORT`、`WS_VSCROLL` |
| IDC_COMBO2 | COMBOBOX | `CBS_DROPDOWNLIST`、`CBS_SORT`、`WS_VSCROLL` |
| IDC_EDIT1 / IDC_EDIT2 / IDC_EDIT4 | EDITTEXT | `ES_READONLY`（只读显示） |
| IDC_SLIDER1 / IDC_SLIDER2 | Trackbar | `TBS_BOTH`、`TBS_NOTICKS` |
| IDC_RADIO1 | Radio | `WS_GROUP`（组起始） |

#### 4.4.3 播放对话框（资源属性）
| 控件 | 类型 | 属性 |
|---|---|---|
| IDC_OCX1 | ActiveX | Windows Media Player CLSID：`{6BF52A52-394A-11D3-B153-00C04F79FAA6}` |

---

### 4.5 Caption 列表（对话框、菜单、按钮文本）

#### 4.5.1 菜单 Caption
| 菜单 | Caption | 命令ID |
|---|---|---:|
| 模拟(&M) | 参数设置(&S) | ID_SIM_SETTINGS |
| 模拟(&M) | 播放开发录像(&V) | ID_SIM_PLAYVIDEO |

#### 4.5.2 参数设置对话框 Caption（主要控件）
| 控件 | ID | Caption |
|---|---:|---|
| Radio | IDC_RADIO1 | 尺寸10 |
| Radio | IDC_RADIO2 | 尺寸20 |
| Radio | IDC_RADIO3 | 尺寸30 |
| Button | IDC_BTN_START | 开始运动 |

#### 4.5.3 播放对话框 Caption
| 控件 | ID | Caption |
|---|---:|---|
| Button | IDC_BUTTON1 | 播放 |
| Button | IDC_BUTTON2 | 关闭 |
| ActiveX | IDC_OCX1 | （无） |

---

## 5. 构建与运行说明
1. 使用 Visual Studio 打开解决方案；
2. 选择平台：`x64`（推荐）；
3. 将 `Narration.avi` 放入输出目录：
   - Debug：`x64\Debug\Narration.avi`
   - Release：`x64\Release\Narration.avi`
4. 运行程序，通过菜单“模拟 -> 播放开发录像”即可在对话框内播放。

---

## 6. 常见问题与排查
- 现象：WMP 控件在“准备就绪/正在打开”之间反复切换
  - 原因：视频路径不正确（相对路径/工作目录不一致）或编码不兼容
  - 解决：使用 exe 同目录绝对路径设置 URL；确认 `Narration.avi` 能被 WMP 播放

---

## 7. 改进方向
- 提供暂停/重置/速度调节
- 更稳定的数值积分方法（如 Runge-Kutta）
- 视频播放增加“文件不存在/格式不支持”的用户提示
